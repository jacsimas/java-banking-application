name: appwithpostgresql
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - "5431:5432"
    volumes:
      - ./services/migration/data:/var/lib/postgresql/data
